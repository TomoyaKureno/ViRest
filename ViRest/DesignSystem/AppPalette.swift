import SwiftUI

enum AppPalette {
    static let backgroundTop = Color(red: 0.05, green: 0.10, blue: 0.18)
    static let backgroundBottom = Color(red: 0.02, green: 0.08, blue: 0.14)

    static let auroraA = Color(red: 0.12, green: 0.75, blue: 0.80)
    static let auroraB = Color(red: 0.34, green: 0.52, blue: 0.96)
    static let auroraC = Color(red: 0.96, green: 0.58, blue: 0.25)

    static let accent = Color(red: 0.10, green: 0.82, blue: 0.74)
    static let accentSecondary = Color(red: 0.96, green: 0.64, blue: 0.26)

    static let card = Color.white.opacity(0.08)
    static let cardBorder = Color.white.opacity(0.18)
    static let textPrimary = Color.white
    static let textSecondary = Color.white.opacity(0.74)
}
