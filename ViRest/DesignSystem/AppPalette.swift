import SwiftUI

enum AppPalette {
    static let backgroundTop = Color.richBlack
    static let backgroundBottom = Color.black

    static let auroraA = Color.vibrantGreen
    static let auroraB = Color.slateGray
    static let auroraC = Color.white.opacity(0.18)

    static let accent = Color.vibrantGreen
    static let accentSecondary = Color.slateGray

    static let card = Color.white.opacity(0.07)
    static let cardBorder = Color.white.opacity(0.16)
    static let textPrimary = Color.white
    static let textSecondary = Color.slateGray
}
